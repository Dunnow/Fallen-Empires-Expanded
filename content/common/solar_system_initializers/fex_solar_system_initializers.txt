fex_shroud_tear = {
	class = "rl_standard_stars"

	planet = {
		count = 1
		class = star
		orbit_distance = 0
		orbit_angle = 1
		size = { min = 20 max = 30 }
		has_ring = no
	}

	change_orbit = 45

	planet = {
		count = { min = 2 max = 10 }
		orbit_distance = 20
		orbit_angle = { min = 90 max = 270 }

		change_orbit = @base_moon_distance

		moon = {
			count = { min = 0 max = 1 }
			orbit_angle = { min = 90 max = 270 }
			orbit_distance = 5
		}
	}

	init_effect = {
		set_star_flag = fex_shroud_tear_system
		star = {
			create_ambient_object = {
				type = "extradimensional_2"
				location = this
			}
			last_created_ambient_object = {
				set_ambient_object_flag = extradimensional_system_effect_2
				set_location = {
					target = prev
					distance = 0
					angle = random
				}
			}
		}
		every_system_planet = {
			limit = {
				OR = {
					is_colonizable = yes
					is_planet_class = pc_barren
					is_planet_class = pc_barren_cold
					is_planet_class = pc_molten
					is_planet_class = pc_toxic
					is_planet_class = pc_frozen
				}
			}
			change_pc = pc_shrouded
			reroll_planet_modifiers = yes
		}
		create_fe_citadel = yes
		starbase = {
			# Two notes
			# Giving the fleet to the FE unbalances things like the war in heaven
			# How to stop the ai using the fleets? It wants to merge and move them.
			# Maybe give the fleets to another country?
			#create_fallen_empire_fleet = yes
			fex_create_shroud_tear_defense_fleet = yes
		}
	}
}