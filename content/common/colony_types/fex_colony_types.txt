col_fex_exodite_age = {
	icon = GFX_fex_colony_type_exodite_world

	potential = {
		exists = owner
		owner = {
			is_country_type = primitive
			has_pre_ftl_age = fex_exodite_age
		}
	}

	weight_modifier = {
		base = 1000
	}
}

# Fallen Empire Colony
col_fex_fe_frontier_world = {
	icon = GFX_colony_type_normal_colony

	potential = {
		is_capital = no
		exists = owner
		owner = { is_fallen_empire = yes }
		fex_is_bespoke_fe_world = no
		# This inits weirdly because the game doesn't seem to have loaded the pops yet when setting colony designations. It will resolve itself on the next month tick
		count_owned_pop = {
			count < 20
		}
	}

	planet_modifier = {
		planet_stability_add = 5
	}

	weight_modifier = {
		base = 2000
	}
}